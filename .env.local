NEXT_PUBLIC_API_URL=https://amaretto-upward-handful.ngrok-free.dev
NEXT_PUBLIC_CLIENT_ID=DEMO
# NOTE: n8n itself isn't tunneled/publicly exposed yet (only the backend API is,
# via ngrok). This still points at localhost and will not work from a deployed
# frontend until n8n is also exposed (or the frontend is switched to call it
# through the backend's own /api/n8n-proxy routes instead of this webhook directly).
NEXT_PUBLIC_N8N_WEBHOOK_URL=http://localhost:5678/webhook/vmind-chat
NEXT_PUBLIC_TURNSTILE_SITE_KEY="0x4AAAAAADt4SzHzPF9IYUqX"
